<?xml version="1.0" encoding="utf-8"?>


<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    >

    <ProgressBar
        android:id="@+id/checkUpdateProgressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@android:color/transparent"
        android:elevation="10dp"
        android:visibility="visible"
        android:backgroundDimEnabled="false"
        android:bottomBright="@android:color/transparent"
        android:bottomDark="@android:color/transparent"
        android:bottomMedium="@android:color/transparent"
        android:centerBright="@android:color/transparent"
        android:centerDark="@android:color/transparent"
        android:centerMedium="@android:color/transparent"
        android:fullBright="@android:color/transparent"
        android:fullDark="@android:color/transparent"
        android:topBright="@android:color/transparent"
        android:topDark="@android:color/transparent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>